void test(){


	for (i = 1; i <= 12; ++i)
		set_help_image(i);
		
	while (*current){	
 		add_file(*current);
		++current;
	}
	
	for (i=1; i<=12; ++i) {
	
	}
	
	for (i=1; i<=12; ++i) {
	
	}
	
	for (i=1; i<=12; ++i) {
	
	}
	
	if (on_step>0) {
      	for (i=1; i<=12; ++i) {

		}
	}
	
	if (++counter % 200 == 0) {
	
	}
	
	for (i=1; i<=12; ++i)
		highlight_step(i, FALSE);
		
	for (i = 1; i <= 12; ++i)
		set_underlines_off(i);
		
	for (i = 1; i <= 12; ++i){
	
	}
	
	for (i = 1; i <= 12; ++i)
    	select_button(i, setting);
 	}
 	
 	for (i = 0, j = 0; i <= strlen(s); ++i){
 	
 		switch(s[i])
         {
             case '\\':
                 ret[j] = ret[j+1] = s[i];
                 ++j;
                 break;
             default:
                 ret[j] = s[i];
                 break;
         }
         ++j;
 	
 	}

}
